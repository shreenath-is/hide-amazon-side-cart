// Select the cart sidebar element by its ID
const cartSidebar = document.querySelector('#nav-flyout-ewc');

// Hide the sidebar if it exists
if (cartSidebar) {
  cartSidebar.style.display = 'none';
}
